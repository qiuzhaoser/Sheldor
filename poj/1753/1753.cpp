#include <iostream>
#include <vector>
using namespace std;
bool array[4][4];
int main()
{
	char c;
	for( int i = 0; i < 4; i++ )
	{
		for( int j = 0; j < 4; j++ )
		{
			scanf("%c",&ch);
			array[i][j] = 'w' == c ? false:true;
		}
	}
	cout<<"hello world"<<endl;
	return 0;
}
